/*
 * This enum hold all the units, buildings and selectable elements possible.
 * 
 * Units should be from 0 to 1000,
 * Buildings from 1001 to 2000,
 * Everything else from 2001 to int.maxValue
 * 
 */

public enum Names
{
    SCV = 0,
    MARINE = 1,
    COMMAND_CENTER = 1001
}
